# Dissent — one wizard's disagreement attached to a verdict.
metadata:
  package: wizardsofodd
  children:
    - object.value:
        name: Dissent
        children:
          - field.string:
              name: wizardId
              extends: Wizard.id
          - field.string:
              name: reason
              required: true
