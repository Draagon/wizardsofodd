# template.streamFrame — the SSE `error` event.
# Emitted when a wizard's structured response failed to parse twice; the
# council continues with the remaining wizards.
metadata:
  package: wizardsofodd
  children:
    - template.streamFrame:
        name: errorFrame
        eventName: error
        payloadRef: ErrorFramePayload
