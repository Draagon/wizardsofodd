// @generated by @metaobjectsdev/codegen-ts — DO NOT EDIT.
// Source metadata: CouncilTurn (CouncilTurn)
// Customize via CouncilTurn.extra.ts in this directory (additional queries, custom logic).
import { eq, inArray } from "drizzle-orm";

import type { BaseSQLiteDatabase } from "drizzle-orm/sqlite-core";
type Db = BaseSQLiteDatabase<"sync" | "async", unknown>;

import {
  type CouncilTurn,
  CouncilTurnInsertSchema,
  type CouncilTurnPatch,
  councilTurns,
  CouncilTurnUpdateSchema,
} from "./CouncilTurn";
export async function findCouncilTurnById(
  db: Db,
  councilId: string,
): Promise<CouncilTurn | null> {
  const [councilTurn] = await db
    .select()
    .from(councilTurns)
    .where(eq(councilTurns.councilId, councilId))
    .limit(1);
  return councilTurn ?? null;
}
export async function listCouncilTurns(
  db: Db,
  opts?: { limit?: number; offset?: number },
): Promise<CouncilTurn[]> {
  let q = db.select().from(councilTurns).$dynamic();
  if (opts?.limit !== undefined) {
    q = q.limit(opts.limit);
  }
  if (opts?.offset !== undefined) {
    q = q.offset(opts.offset);
  }
  return q;
}
export async function createCouncilTurn(
  db: Db,
  data: unknown,
): Promise<CouncilTurn> {
  const validated = CouncilTurnInsertSchema.parse(data);
  const [councilTurn] = await db
    .insert(councilTurns)
    .values(validated)
    .returning();
  return councilTurn!;
}
export async function updateCouncilTurn(
  db: Db,
  councilId: string,
  patch: CouncilTurnPatch,
): Promise<CouncilTurn | null> {
  const validated = CouncilTurnUpdateSchema.parse(patch);
  // PATCH-5: an empty patch is a no-op — return the current row rather than let
  // Drizzle throw on an empty SET clause.
  if (Object.keys(validated).length === 0) {
    return findCouncilTurnById(db, councilId);
  }
  const [councilTurn] = await db
    .update(councilTurns)
    .set(validated)
    .where(eq(councilTurns.councilId, councilId))
    .returning();
  return councilTurn ?? null;
}
export async function deleteCouncilTurnById(
  db: Db,
  councilId: string,
): Promise<boolean> {
  // Use .returning() unconditionally — supported on SQLite ≥3.35 (covers D1, libsql/Turso)
  // and Postgres. Result is an array of deleted rows; presence implies success.
  const deleted = await db
    .delete(councilTurns)
    .where(eq(councilTurns.councilId, councilId))
    .returning();
  return deleted.length > 0;
}
export async function findCouncilTurnsByCouncil(
  db: Db,
  councilId: string,
): Promise<CouncilTurn[]> {
  return db
    .select()
    .from(councilTurns)
    .where(eq(councilTurns.councilId, councilId));
}

export async function findCouncilTurnsByCouncilIn(
  db: Db,
  councilIds: string[],
): Promise<CouncilTurn[]> {
  if (councilIds.length === 0) {
    return [];
  }
  return db
    .select()
    .from(councilTurns)
    .where(inArray(councilTurns.councilId, councilIds));
}
