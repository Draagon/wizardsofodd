// @generated by @metaobjectsdev/codegen-ts — DO NOT EDIT.
// Source metadata: CouncilTurn (CouncilTurn)
// Customize via CouncilTurn.extra.ts in this directory.
import {
  type InferInsertModel,
  type InferSelectModel,
  relations,
  sql,
} from "drizzle-orm";
import {
  check,
  integer,
  primaryKey,
  real,
  sqliteTable,
  text,
} from "drizzle-orm/sqlite-core";
import { z } from "zod";
import { councils } from "./Council";
import { SourceLens, SourceLensInsertSchema } from "./SourceLens";
import { WizardStanceEnum } from "./enums";

export const councilTurns = sqliteTable(
  "council_turns",
  {
    councilId: text("council_id").notNull(),
    ordinal: integer("ordinal").notNull(),
    round: integer("round").notNull(),
    wizardId: text("wizard_id").notNull(),
    wizardName: text("wizard_name").notNull(),
    kind: text("kind", { enum: ["wizard", "error"] as const }).notNull(),
    stance: text("stance", {
      enum: [
        "supports",
        "opposes",
        "complicates",
        "reframes",
        "abstains",
      ] as const,
    }).notNull(),
    takeMarkdown: text("take_markdown").notNull(),
    oneLineSummary: text("one_line_summary").notNull(),
    confidence: real("confidence").notNull(),
    keyClaims: text("key_claims_json", { mode: "json" })
      .$type<string[]>()
      .notNull(),
    citations: text("citations_json", { mode: "json" }).$type<SourceLens[]>(),
  },
  (table) => [
    primaryKey({ columns: [table.councilId, table.ordinal] }),
    check("chk_council_turns_kind", sql`kind IN ('wizard', 'error')`),
    check(
      "chk_council_turns_stance",
      sql`stance IN ('supports', 'opposes', 'complicates', 'reframes', 'abstains')`,
    ),
  ],
);
export const councilTurnsRelations = relations(councilTurns, ({ one }) => ({
  council: one(councils, {
    fields: [councilTurns.councilId],
    references: [councils.id],
  }),
}));
export type CouncilTurn = InferSelectModel<typeof councilTurns>;
export type CouncilTurnInsert = InferInsertModel<typeof councilTurns>;
export type CouncilTurnUpdate = Partial<CouncilTurnInsert>;
export type CouncilTurnKind = "wizard" | "error";
export { type WizardStance } from "./enums";
export const CouncilTurnInsertSchema = z.object({
  councilId: z.string().min(1).max(16),
  ordinal: z.number().int(),
  round: z.number().int(),
  wizardId: z.string().min(1).max(32),
  wizardName: z.string().min(1).max(128),
  kind: z.enum(["wizard", "error"]),
  stance: WizardStanceEnum,
  takeMarkdown: z.string().min(1),
  oneLineSummary: z.string().min(1).max(140),
  confidence: z.number().min(0).max(1),
  keyClaims: z.array(z.string()),
  citations: z.array(SourceLensInsertSchema).optional(),
});

export const CouncilTurnUpdateSchema = z.object({
  councilId: z.string().min(1).max(16).optional(),
  ordinal: z.number().int().optional(),
  round: z.number().int().optional(),
  wizardId: z.string().min(1).max(32).optional(),
  wizardName: z.string().min(1).max(128).optional(),
  kind: z.enum(["wizard", "error"]).optional(),
  stance: WizardStanceEnum.optional(),
  takeMarkdown: z.string().min(1).optional(),
  oneLineSummary: z.string().min(1).max(140).optional(),
  confidence: z.number().min(0).max(1).optional(),
  keyClaims: z.array(z.string()).optional(),
  citations: z.array(SourceLensInsertSchema).optional().nullable(),
});

/** Typed patch shape for CouncilTurn: every settable field, optional (FR-035 PATCH). A
 * renamed/dropped field is a compile error at every `updateCouncilTurn` call site. */
export type CouncilTurnPatch = z.input<typeof CouncilTurnUpdateSchema>;
/**
 * Metadata constants for CouncilTurn.
 *
 * Use these instead of magic strings so TS catches typos and refactors stay
 * coherent. Each non-dollar-prefixed key is a per-field object carrying
 * name, label, view, optional htmlType/placeholder/helpText, and the
 * RHF-shaped validation rules derived from the field's validator children.
 *
 * Typical usage with the metaobjects React form helper:
 *
 *   import { useEntityForm } from '@metaobjectsdev/react';
 *   const form = useEntityForm(CouncilTurn, CouncilTurnInsertSchema);
 *   <input {...form.input.councilId} />
 */
export const CouncilTurn = {
  $entity: "CouncilTurn",
  $table: "council_turns",
  $path: "/council_turns",
  $apiPrefix: "",
  councilId: {
    name: "councilId",
    label: "Council Id",
    view: "text",
    htmlType: "text",
    rules: {
      required: "Council Id is required",
      maxLength: { value: 16, message: "Must be 16 characters or fewer" },
    },
  },
  ordinal: {
    name: "ordinal",
    label: "Ordinal",
    view: "number",
    htmlType: "number",
    rules: { required: "Ordinal is required" },
  },
  round: {
    name: "round",
    label: "Round",
    view: "number",
    htmlType: "number",
    rules: { required: "Round is required" },
  },
  wizardId: {
    name: "wizardId",
    label: "Wizard Id",
    view: "text",
    htmlType: "text",
    rules: {
      required: "Wizard Id is required",
      maxLength: { value: 32, message: "Must be 32 characters or fewer" },
    },
  },
  wizardName: {
    name: "wizardName",
    label: "Wizard Name",
    view: "text",
    htmlType: "text",
    rules: {
      required: "Wizard Name is required",
      maxLength: { value: 128, message: "Must be 128 characters or fewer" },
    },
  },
  kind: {
    name: "kind",
    label: "Kind",
    view: "text",
    htmlType: "text",
    rules: { required: "Kind is required" },
  },
  stance: {
    name: "stance",
    label: "Stance",
    view: "text",
    htmlType: "text",
    rules: { required: "Stance is required" },
  },
  takeMarkdown: {
    name: "takeMarkdown",
    label: "Take Markdown",
    view: "text",
    htmlType: "text",
    rules: { required: "Take Markdown is required" },
  },
  oneLineSummary: {
    name: "oneLineSummary",
    label: "One Line Summary",
    view: "text",
    htmlType: "text",
    rules: {
      required: "One Line Summary is required",
      maxLength: { value: 140, message: "Must be 140 characters or fewer" },
    },
  },
  confidence: {
    name: "confidence",
    label: "Confidence",
    view: "number",
    htmlType: "number",
    rules: { required: "Confidence is required" },
  },
  keyClaims: {
    name: "keyClaims",
    label: "Key Claims",
    view: "text",
    htmlType: "text",
    rules: { required: "Key Claims is required" },
  },
  citations: {
    name: "citations",
    label: "Citations",
    view: "text",
    htmlType: "text",
  },
} as const;
import type { FilterAllowlist } from "@metaobjectsdev/runtime-ts/drizzle-fastify";

export const CouncilTurnFilterAllowlist = {} as const satisfies FilterAllowlist;
import type { SortAllowlist } from "@metaobjectsdev/runtime-ts/drizzle-fastify";

export const CouncilTurnSortAllowlist = {} as const satisfies SortAllowlist;
export type CouncilTurnFilter = {
  limit?: number;
  offset?: number;
  sort?: string;
  or?: CouncilTurnFilter[];
  and?: CouncilTurnFilter[];
};
