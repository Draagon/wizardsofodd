// @generated by @metaobjectsdev/codegen-ts — DO NOT EDIT.
// Source metadata: Wizard (Wizard)
// Customize via Wizard.extra.ts in this directory.
import { z } from "zod";

export interface Wizard {
  id: string;
  name: string;
  title: string;
  technique: string;
  bickersWith: string[];
  usesSearch: boolean;
  order?: number;
  techniqueBlurb: string;
  defaultEnabled: boolean;
}
export const WizardInsertSchema = z.object({
  id: z.string().min(1).max(32),
  name: z.string().min(1).max(128),
  title: z.string().min(1).max(128),
  technique: z.string().min(1).max(256),
  bickersWith: z.array(z.string()),
  usesSearch: z.boolean(),
  order: z.number().int().optional(),
  techniqueBlurb: z.string().min(1).max(280),
  defaultEnabled: z.boolean(),
});
